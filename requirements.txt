# Camera Test Bench – Python dependencies
# Install with: pip install -r requirements.txt

# Core computer vision
opencv-python-headless>=4.7.0

# Basler camera SDK Python bindings
# Install via: pip install pypylon
# Or download the installer from https://github.com/basler/pypylon
pypylon>=3.0.0

# Numerical computing (usually already present)
numpy>=1.24.0

# PyQt5 — required for v2 GUI (test_bench_app_v2.py)
PyQt5>=5.15.0